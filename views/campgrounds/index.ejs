<!-- since it will always start looking from views directory -->
<!-- When you use ' layout('/layouts/main')' in your index page, EJS looks for the
     main.ejs file directly inside the views/layouts/ directory. and uses absolute path from views
    but when u include a file it will always use relative path in which the template file like
main.ejs is always reference point -->
<% layout('/layouts/main') %>




<h1>All Campgrounds</h1>
<div>
    <a href="/campgrounds/new">Add Campground</a>
</div>
<% for (let campground of campgrounds){%>
<div class="card" mb-3>
    <div class="row">
        <div class="col-md-4">
            <img class="img-fluid" alt="" src="<%=campground.images[0].url%>">
        </div>
        <div class="col-md-8">
            <div class="card-body">
                <h5 class="card-title"><%= campground.title %> </h5>
                <p class="card-text"><%= campground.description %></p>
                <p class="card-text">
                    <small class="text-muted"><%= campground.location%></small>
                </p>
                <a class="btn btn-primary" href="/campgrounds/<%=campground._id%>">View <%=campground.title%></a>
            </div>
        </div>
    </div>
</div>
<% }%>

